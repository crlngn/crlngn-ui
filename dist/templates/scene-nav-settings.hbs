<div>

  <div class="form-group">
    <label for="sceneNavEnabled">{{fields.sceneNavEnabled.label}}</label>
    <input type="checkbox" name="sceneNavEnabled" {{#if sceneNavEnabled}}checked{{/if}} />
    <p class="hint">{{{fields.sceneNavEnabled.hint}}}</p> 
  </div>

  <div class="form-group">
    <label for="navFoldersEnabled">{{fields.navFoldersEnabled.label}}</label>
    <input type="checkbox" name="navFoldersEnabled" {{#if navFoldersEnabled}}checked{{/if}} />
    <p class="hint">{{{fields.navFoldersEnabled.hint}}}</p> 
  </div>

  <div class="form-group">
    <label for="navFoldersForPlayers">{{fields.navFoldersForPlayers.label}}</label>
    <input type="checkbox" name="navFoldersForPlayers" {{#if navFoldersForPlayers}}checked{{/if}} />
    <p class="hint">{{{fields.navFoldersForPlayers.hint}}}</p> 
  </div>

  <div class="form-group">
    <label for="showFolderListOnClick">{{fields.showFolderListOnClick.label}}</label>
    <input type="checkbox" name="showFolderListOnClick" {{#if showFolderListOnClick}}checked{{/if}} />
    <p class="hint">{{{fields.showFolderListOnClick.hint}}}</p> 
  </div>

  <div class="form-group">
    <label for="showNavOnHover">{{fields.showNavOnHover.label}}</label>
    <input type="checkbox" name="showNavOnHover" {{#if showNavOnHover}}checked{{/if}} />
    <p class="hint">{{{fields.showNavOnHover.hint}}}</p> 
  </div>

</div>
