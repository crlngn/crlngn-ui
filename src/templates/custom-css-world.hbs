<section
  class="form-content tab {{#if tab.active}} active{{/if}}" data-group="{{tab.group}}" data-tab="{{tab.id}}"
>
  <p class="hint on">{{worldHint}}</p>
  <div class="code-editor-wrapper">
    <code-mirror name="worldStyles" class="crlngn-code-editor" language="plain" indent="2"></code-mirror>
  </div>
</section>
